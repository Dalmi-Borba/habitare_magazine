<!DOCTYPE html>
<html lang="pt-BR">
  <head>
    <%- include('../partials/head', { pageTitle, metaDescription }) %>
    <link rel="stylesheet" href="/css/admin.css" />
  </head>
  <body class="admin-body">
    <main class="admin-main login-main">
      <section class="admin-card login-card">
        <h2>Login Administrativo</h2>
        <% if (error === 'credenciais-invalidas') { %>
        <div class="admin-alert login-alert">
          Credenciais inválidas. Tente novamente.
        </div>
        <% } %>
        <form class="admin-form login-form" action="/admin/login" method="POST">
          <input type="hidden" name="redirect" value="<%= redirect %>" />
          <label>
            Usuário
            <input type="text" name="username" required autofocus placeholder="Digite seu usuário" />
          </label>
          <label>
            Senha
            <input type="password" name="password" required placeholder="Digite sua senha" />
          </label>
          <button class="button primary" type="submit">Entrar</button>
        </form>
      </section>
    </main>
  </body>
</html>

